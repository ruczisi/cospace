# Cospace 产品需求文档 v2.0

## 智能方案/文案工作台 - 重构版

> 状态：设计中  
> 更新日期：2026-05-12  
> 目标版本：v2.0

---

## 1. 产品定位

### 1.1 核心理念

**Cospace 不是 Agent，而是 Agent 驱动的工作平台。**

为方案/文案工作者提供一个集成化的工作台，固化个人工作方法，让 Agent 成为驱动工具的基础设施。

### 1.2 目标用户

- 经常撰写方案、文案、报告的知识工作者
- 需要管理多个并行项目的项目经理
- 希望沉淀工作方法、积累知识资产的创作者

### 1.3 与现有产品的差异

| 产品类型 | 特点 | Cospace 的差异 |
|---------|------|---------------|
| 通用 Agent 工具 | 直接聊天，对话驱动 | **工作台结构化**：任务-阶段-产出，有明确边界 |
| RPA/自动化工具 | 自动执行流程 | **人机协同**：人主导关键决策，Agent 执行辅助 |
| 文档协作工具 | 在线编辑文档 | **Agent 驱动**：Agent 理解上下文，主动调用工具 |
| 项目管理工具 | 看板、任务追踪 | **面向内容创作**：专为文案/方案场景设计 |

---

## 2. 核心概念

### 2.1 三层架构

```
┌─────────────────────────────────────────────────────────────────┐
│  第一层：任务管理（多任务并行）                                   │
├─────────────────────────────────────────────────────────────────┤
│  每个任务 = 独立工作目录 + 独立 Agent 会话                        │
│                                                                  │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐              │
│  │  任务 A      │  │  任务 B      │  │  任务 C      │              │
│  │  (活跃)      │  │  (活跃)      │  │  (暂停)      │              │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤              │
│  │ 工作目录     │  │ 工作目录     │  │ 工作目录     │              │
│  │ /项目/贵港   │  │ /项目/容县   │  │ /项目/某汇报 │              │
│  ├─────────────┤  ├─────────────┤  ├─────────────┤              │
│  │ Agent 会话   │  │ Agent 会话   │  │ Agent 会话   │              │
│  │ (Claude)     │  │ (Codex)      │  │ [已暂停]     │              │
│  └─────────────┘  └─────────────┘  └─────────────┘              │
│                                                                  │
│  切换任务 = 切换工作区 + 切换 Agent 上下文                       │
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  第二层：Agent 驱动层（上下文注入）                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  工作台 → 注入上下文 → Agent → 调用工具 → 返回结果              │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ Agent Session                                               ││
│  │                                                             ││
│  │ 系统提示词（自动注入）：                                      ││
│  │ ┌──────────────────────────────────────────────────────────┐││
│  │ │ 当前任务：贵港供销社方案                                    │││
│  │ │ 当前阶段：4.框架构思                                       │││
│  │ │ 阶段目标：产出方案框架大纲                                  │││
│  │ │ 工作目录：/项目/贵港供销社方案                              │││
│  │ │ 可用工具：/search /template /data /skill                  │││
│  │ └──────────────────────────────────────────────────────────┘││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│  第三层：工作流界面（配置驱动）                                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌────────┐  ┌────────────────────────────────┐  ┌──────────┐ │
│  │任务    │  │ Agent 对话区                   │  │ 知识资产 │ │
│  │列表    │  │                                │  │          │ │
│  │       │  │ 用户：帮我构思方案框架           │  │ 📚 模板  │ │
│  │ ▶ A   │  │                                │  │ 📊 数据  │ │
│  │ ▶ B   │  │ Agent：/search 查找参考案例     │  │ 📄 参考  │ │
│  │ ⏸ C   │  │ ...                            │  │          │ │
│  │       │  │                                │  │          │ │
│  └────────┘  └────────────────────────────────┘  └──────────┘ │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────────┐│
│  │ 工作流导航（由 workflow.md 定义，支持 2-N 个阶段）            ││
│  │ ✓ ① 需求确认  ▶ ② 框架构思  ○ ③ 内容撰写  ○ ④ 审核定稿    ││
│  └─────────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────────┘
```

### 2.2 配置体系（混合模式）

#### 全局配置（用户级）

```
~/.cospace/config.json                  # 用户级全局配置
或
/工作区/.cospace/config.json            # 项目级全局配置

{
  "assets": {
    "templates": "~/notebook/templates",    # 文档模板库
    "data": "~/notebook/data",              # 数据文件
    "references": "~/notebook/references",   # 参考资料
    "cases": "~/notebook/cases"             # 历史案例
  },
  "workflows": "~/.cospace/workflows/",     # 工作流模板库
  "defaultWorkflow": "standard-4stage",   # 默认工作流
  "agent": {
    "type": "claude",
    "autoStart": true
  }
}
```

#### 任务级配置（覆盖全局）

```
/项目/任务A/.cospace/
├── task.json                       # 当前进度、Agent会话状态
├── workflow.md                     # 工作流定义（可复制全局或自定义）
└── context.jsonl                   # Agent对话历史（可选）
```

**配置继承规则**：
- 知识资产路径 → 全局配置（所有任务共享）
- 工作流定义 → 任务可覆盖（复制全局模板或自定义）
- 当前进度 → 任务级（必须跟随任务）
- Agent会话 → 任务级（每个任务独立）

### 2.3 文档组织

```
/项目/贵港供销社方案/                    # 任务根目录
├── .cospace/                          # 工作台元数据（隐藏）
│   ├── task.json                       # 当前阶段、进度
│   └── workflow.md                     # 工作流定义
│
├── 01-需求确认/                        # 阶段1目录（自动创建）
│   └── 需求确认清单.md
│
├── 02-框架构思/                        # 阶段2目录（自动创建）
│   ├── 框架-v1.md
│   ├── 框架-v2.md
│   └── 选定框架.md
│
├── 03-内容撰写/                        # 阶段3目录
│
├── 04-审核定稿/                        # 阶段4目录
│
└── 素材/                               # 用户自建目录（可选）
    ├── 贵港供销社介绍.pdf
    └── 成本测算.xlsx
```

**关键设计**：
- 阶段目录根据 `workflow.md` 中的 `outputs.path` 自动创建
- 用户可自建 `素材/`、`参考资料/` 等目录，工作台忽略
- 删除 `.cospace/` 后仍是完整文档，只是缺少工作台功能
- 任务可整体移动/复制/归档，所有元数据跟随

---

## 3. 功能规格

### 3.1 任务管理（多任务并行）

#### 3.1.1 任务生命周期

```typescript
interface Task {
  id: string;
  name: string;
  path: string;                    // 工作目录绝对路径
  
  status: 'draft' | 'active' | 'paused' | 'completed' | 'archived';
  
  // 当前阶段
  currentStage: {
    index: number;                 // 当前阶段索引
    id: string;                    // 阶段标识
    name: string;                  // 阶段名称
  };
  
  // Agent会话
  agent: {
    sessionId: string;
    type: 'claude' | 'codex' | 'custom';
    status: 'idle' | 'thinking' | 'executing';
  };
  
  // 时间追踪
  createdAt: number;
  lastAccessed: number;
}
```

#### 3.1.2 任务操作

| 操作 | 说明 |
|------|------|
| 创建任务 | 选择工作流模板 → 输入名称 → 创建工作目录 → 初始化配置 |
| 启动任务 | 启动 Agent 会话 → 自动 cd 到任务目录 → 注入上下文 |
| 暂停任务 | 保留 Agent 会话状态，降低资源占用 |
| 恢复任务 | 恢复 Agent 会话，继续之前的工作 |
| 切换任务 | 切换焦点到指定任务，Agent 上下文自动切换 |
| 关闭任务 | 保存状态，关闭 Agent 会话 |
| 归档任务 | 移动到归档目录，保留完整元数据 |
| 删除任务 | 删除任务目录（可选择保留文档或完全删除） |

#### 3.1.3 多任务界面

```
┌─────────────────────────────────────────────────────────────┐
│ Cospace  [任务A] [任务B] [+]                     [👤] [⚙️]  │
│         Ctrl+1   Ctrl+2                                     │
├─────────────────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────────────────────────────┐ ┌──────────┐ │
│ │ 任务   │ │      当前：任务A - 阶段2/4       │ │ 知识资产 │ │
│ │ 列表   │ │      框架构思（进行中）           │ │          │ │
│ │       │ └────────────────────────────────┘ │          │ │
│ │ ▶ A   │                                  │          │ │
│ │ 阶段2 │                                  │          │ │
│ │       │  Agent 对话区                     │          │ │
│ │ ▶ B   │                                  │          │ │
│ │ 阶段6 │                                  │          │ │
│ │       │                                  │          │ │
│ │ ⏸ C   │                                  │          │ │
│ │ ...   │                                  │          │ │
│ │       │                                  │          │ │
│ └────────┘                                  └──────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 工作流系统（配置驱动）

#### 3.2.1 工作流定义（workflow.md）

```markdown
# 贵港供销社方案工作流

## 元数据
- name: 贵港供销社方案
- type: proposal
- version: 1.0
- description: 面向供销社的供应链合作方案

## 阶段定义

### 阶段1：需求确认
- id: demand
- name: 需求确认
- description: 明确客户核心诉求和项目边界
- outputs:
    - name: 需求确认清单
      path: 01-需求确认/demand-checklist.md
      template: templates/demand-checklist.md
- agent_context: |
    当前阶段：需求确认
    任务：与贵港供销社确认项目需求
    你需要帮我：
    1. 梳理客户的核心诉求
    2. 明确项目边界和交付物
    3. 识别潜在风险和注意事项
    4. 产出需求确认清单

### 阶段2：框架构思
- id: framework
- name: 框架构思
- description: 确定方案整体结构和逻辑
- depends: demand
- outputs:
    - name: 方案框架
      path: 02-框架构思/framework.md
- agent_context: |
    当前阶段：框架构思
    上一阶段产出：{{demand.outputs[0].path}}
    任务：基于需求确认清单，构思方案的整体框架

### 阶段3：内容撰写
- id: draft
- name: 内容撰写
- description: 完成方案正文内容
- depends: framework
- outputs:
    - name: 方案初稿
      path: 03-内容撰写/draft.md

### 阶段4：审核定稿
- id: review
- name: 审核定稿
- description: 检查内容、格式，输出最终版本
- depends: draft
- outputs:
    - name: 最终方案
      path: 04-审核定稿/方案-最终版.docx
```

#### 3.2.2 工作流配置语法

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 阶段唯一标识（URL-friendly） |
| name | string | 阶段显示名称 |
| description | string | 阶段说明 |
| depends | string/string[] | 依赖的前置阶段 |
| outputs | OutputConfig[] | 阶段产出文档定义 |
| outputs.path | string | 产出文档相对路径（自动创建目录） |
| outputs.template | string | 模板路径（可选） |
| agent_context | string | Agent 系统提示词模板 |

### 3.3 Agent 驱动层

#### 3.3.1 上下文注入

工作台自动在每次对话前注入系统提示词：

```markdown
## 系统上下文

当前任务：{{task.name}}
任务类型：{{workflow.type}}
当前阶段：{{stage.name}}（{{stage.index}}/{{workflow.stages.length}}）
阶段目标：{{stage.description}}
工作目录：{{task.path}}

可用工具：
- /search [query] - 搜索知识库
- /template [name] - 引用模板
- /data [file] - 读取数据文件
- /skill [name] - 调用工作流技能
- /stage [next|prev|jump [id]] - 切换阶段

---

用户输入：
```

#### 3.3.2 工具调用机制

```typescript
// Agent 通过特殊语法调用工作台工具

// 1. 搜索知识库
/search query:供应链案例 category:模板 limit:5

// 2. 引用模板
/template load:方案框架-供应链

// 3. 读取数据
/data read:市场调研.xlsx sheet:2026

// 4. 切换阶段
/stage next

// 工作台解析指令 → 执行 → 返回结果给 Agent → Agent 继续处理
```

### 3.4 知识资产库

#### 3.4.1 资产类型

| 类型 | 路径配置 | 用途 |
|------|---------|------|
| templates | config.assets.templates | 文档模板（Markdown/Word） |
| data | config.assets.data | 数据文件（Excel/CSV/JSON） |
| references | config.assets.references | 参考资料（PDF/文档） |
| cases | config.assets.cases | 历史案例 |
| scripts | config.assets.scripts | 自动化脚本 |

#### 3.4.2 资产引用

```
右侧面板：知识资产
├── 📚 模板库
│   ├── 需求确认清单模板
│   ├── 方案框架模板
│   └── 项目计划模板
│
├── 📊 数据文件
│   ├── 成本测算表.xlsx
│   └── 市场调研数据.csv
│
├── 📄 参考资料
│   ├── 行业政策汇编.pdf
│   └── 竞品分析报告.md
│
└── 📁 历史案例
    ├── 容县沙田柚方案
    └── 兴安农产品方案
```

---

## 4. 第一阶段 MVP 范围

### 4.1 范围定义

**目标**：可运行的多任务 Agent 驱动工作台

### 4.2 包含功能

#### P0 - 核心功能（必须有）

| 模块 | 功能 | 说明 |
|------|------|------|
| 任务管理 | 创建/删除任务 | 支持从模板创建工作流 |
| 任务管理 | 任务列表展示 | 显示当前阶段、状态 |
| 任务管理 | 切换任务 | 快捷键 Ctrl+1/2/3 |
| 工作流 | 解析 workflow.md | 支持 2-N 个自定义阶段 |
| 工作流 | 阶段导航 | 高亮当前阶段，显示进度 |
| 工作流 | 阶段目录自动创建 | 根据 outputs.path |
| Agent | 多会话管理 | 每个任务独立 Agent 会话 |
| Agent | 上下文注入 | 自动注入任务、阶段信息 |
| Agent | 基础对话 | 用户输入/Agent 输出 |
| 配置 | 全局配置读取 | ~/.cospace/config.json |
| 配置 | 任务级配置 | 任务/.cospace/task.json |

#### P1 - 重要功能（应该有）

| 模块 | 功能 | 说明 |
|------|------|------|
| Agent | 暂停/恢复任务 | 保留会话状态 |
| Agent | 会话历史 | 保存对话到 context.jsonl |
| 工作流 | 阶段跳转 | /stage next/prev/jump |
| 知识库 | 模板列表展示 | 右侧面板显示可用模板 |
| 知识库 | 基础搜索 | /search 指令支持本地文件搜索 |
| 界面 | 快捷操作 | Ctrl+S 保存、Ctrl+E 编辑 |
| 界面 | 响应式设计 | 支持窗口大小调整 |

#### P2 - 增值功能（可以有）

| 模块 | 功能 | 说明 |
|------|------|------|
| 知识库 | 模板预览 | 点击模板预览内容 |
| 知识库 | 数据文件读取 | /data 读取 Excel/CSV |
| 工作流 | 工作流编辑器 | 可视化编辑 workflow.md |
| 统计 | 时间追踪 | 各阶段耗时统计 |
| 导出 | 任务导出 | 导出任务为压缩包 |

### 4.3 明确不做

| 功能 | 原因 | 计划版本 |
|------|------|---------|
| 可视化工作流编辑器 | 先手动编辑 workflow.md | v2.1+ |
| 复杂工具系统 | 先纯对话，工具用自然语言 | v2.1+ |
| 协作功能 | 单人先用 | v2.2+ |
| 云端同步 | 纯本地优先 | v2.2+ |
| 移动端支持 | 桌面优先 | v3.0+ |

### 4.4 技术实现要点

#### 4.4.1 配置加载流程

```
1. 启动时加载全局配置
   ~/.cospace/config.json
   
2. 扫描任务目录
   遍历 /项目/*/.cospace/task.json
   
3. 加载任务配置
   读取 task.json + workflow.md
   
4. 合并配置
   任务级覆盖全局配置
```

#### 4.4.2 Agent 会话管理

```
创建任务
   ↓
创建工作目录 + .cospace/
   ↓
启动 Agent Session
   - 启动 PTY
   - cd 到任务目录
   - 启动 claude/codex
   ↓
注入系统提示词（首次对话）
   ↓
用户与 Agent 对话
```

#### 4.4.3 阶段切换

```
用户点击阶段N
   ↓
工作台：
   - 更新 task.json 中的 currentStage
   - 检查阶段目录是否存在，不存在则创建
   - 更新 Agent 系统提示词中的当前阶段
   - 可选：发送提示给 Agent "进入阶段N"
   ↓
Agent：继续对话，基于新阶段上下文
```

---

## 5. 界面原型

### 5.1 主界面布局

```
┌─────────────────────────────────────────────────────────────────────────┐
│ Cospace ▼  贵港供销社方案                    [+] [搜索] [👤] [⚙️]      │
├─────────────────────────────────────────────────────────────────────────┤
│ ┌────────┐ ┌────────────────────────────────────────┐ ┌──────────────┐ │
│ │任务    │ │ 阶段2/4：框架构思（进行中）             │ │   知识资产   │ │
│ │列表    │ │ 目标：确定方案整体结构                   │ │              │ │
│ │       │ │                                        │ │ 📚 模板      │ │
│ │ ▶ 贵港│ │ ┌────────────────────────────────────┐ │ │ │ • 方案框架   │ │
│ │ 供销社│ │ │ 💬 对话 (3)  🚀 Agent 运行  📜 历史 │ │ │ • 需求清单   │ │
│ │ 阶段2 │ │ ├────────────────────────────────────┤ │ │              │ │
│ │       │ │ │                                    │ │ │ 📊 数据      │ │
│ │ ▶ 容县│ │ │ 用户：帮我构思方案框架              │ │ │ │ • 成本测算   │ │
│ │ 沙田柚│ │ │                                    │ │ │ │ • 市场调研   │ │
│ │ 阶段6 │ │ │ Agent：我来帮您构思...             │ │ │ │              │ │
│ │       │ │ │ ▶ 思考过程 (128 字)                │ │ │ 📄 参考      │ │
│ │ ⏸ 某  │ │ │ ┌──────────────────────────────┐   │ │ │ │ • 行业政策   │ │
│ │ 汇报  │ │ │ │  1. 先分析需求...            │   │ │ │ │ • 竞品分析   │ │
│ │ 阶段2 │ │ │ │  2. 再对比案例...            │   │ │ │ [浏览全部]   │ │
│ └────────┘ │ │ └──────────────────────────────┘   │ │ └──────────────┘ │
│            │ │                                    │ │                  │
│            │ ┌────────────────────────────────┐   │ │                  │
│            │ │ [输入框...                ] [➤]│   │ │                  │
│            │ └────────────────────────────────┘   │ │                  │
│            │ ● Agent 运行中  claude    [停止]     │ │                  │
│            └────────────────────────────────────────┘                  │
│ ┌─────────────────────────────────────────────────────────────────────┐│
│ │ 工作流导航                                                          ││
│ │ ✓ ① 需求确认  ▶ ② 框架构思  ○ ③ 内容撰写  ○ ④ 审核定稿            ││
│ └─────────────────────────────────────────────────────────────────────┐│
└────────────────────────────────────────────────────────────────────────┘
```

**布局说明**：
- 对话区域采用自适应 flex 布局，`flex-1` 填充可用空间
- 消息列表 `overflow-y-auto` + `min-h-0`，内容溢出时出现滚动条，不顶出输入框
- Agent 状态统一收拢在底部状态栏，以彩色圆点+文字展示
- 思考过程与回复文本分离，默认折叠，点击展开查看

### 5.2 新建任务流程

```
[+] 新建任务
    ↓
┌─────────────────────────────────────┐
│ 新建任务                            │
├─────────────────────────────────────┤
│                                     │
│ 选择工作流模板：                     │
│ ┌─────────────────────────────────┐   │
│ │ • 标准方案（4阶段）           │   │
│ │ • 快速产出（2阶段）           │   │
│ │ • 完整流程（8阶段）           │   │
│ │ • [从 workflow.md 导入]       │   │
│ └─────────────────────────────────┘   │
│                                     │
│ 任务名称：____________________      │
│                                     │
│ 保存位置：~/项目/______________      │
│                                     │
│ [取消]              [创建任务]      │
│                                     │
└─────────────────────────────────────┘
```

---

## 6. 技术实现建议

### 6.1 架构图

```
Cospace Desktop App
├── Frontend (React + TypeScript)
│   ├── TaskManager            # 任务管理
│   ├── WorkflowNavigator      # 工作流导航
│   ├── AgentChat              # Agent 对话界面
│   ├── AssetPanel             # 知识资产面板
│   └── Workspace              # 工作区布局
│
├── Backend (Rust + Tauri)
│   ├── TaskService            # 任务 CRUD
│   ├── WorkflowParser         # workflow.md 解析
│   ├── ConfigManager          # 配置管理
│   ├── AgentManager           # Agent 会话管理
│   └── FileWatcher            # 文件监听
│
└── External
    ├── Claude/Codex (PTY)    # Agent 进程
    └── Knowledge Assets      # 本地知识库
```

### 6.2 关键技术决策

| 决策 | 选择 | 原因 |
|------|------|------|
| 配置格式 | Markdown (workflow.md) | 用户友好，易编辑，可版本控制 |
| 配置格式 | JSON (task.json) | 机器友好，性能高 |
| Agent 通信 | PTY/STDIO | 复用现有 Cospace 实现 |
| 状态管理 | 文件系统优先 | 数据持久化，用户可控 |
| 缓存 | 内存 + 文件 | 重启后可恢复 |

### 6.3 文件结构

```
cospace/
├── src/
│   ├── components/
│   │   ├── TaskList.tsx
│   │   ├── WorkflowNavigator.tsx
│   │   ├── AgentChat.tsx
│   │   ├── AssetPanel.tsx
│   │   └── Workspace.tsx
│   ├── services/
│   │   ├── taskService.ts
│   │   ├── workflowParser.ts
│   │   ├── configManager.ts
│   │   └── agentManager.ts
│   ├── types/
│   │   └── index.ts
│   ├── stores/
│   │   └── appStore.ts
│   └── App.tsx
├── src-tauri/
│   └── src/
│       ├── main.rs
│       ├── commands/
│       │   ├── task.rs
│       │   ├── workflow.rs
│       │   ├── config.rs
│       │   └── agent.rs
│       └── lib/
├── docs/
│   └── PRD-v2.0.md
└── templates/
    └── workflows/
        ├── standard-4stage.md
        ├── quick-2stage.md
        └── complete-8stage.md
```

---

## 7. 验收标准

### 7.1 MVP 验收清单

| 功能 | 验收标准 | 优先级 |
|------|---------|--------|
| 创建任务 | 能从模板创建任务，生成正确目录结构 | P0 |
| 任务切换 | 能切换任务，Agent 上下文正确切换 | P0 |
| 工作流解析 | 能正确解析 workflow.md，显示阶段导航 | P0 |
| Agent 对话 | 能与 Agent 正常对话，上下文正确注入 | P0 |
| 阶段切换 | 能切换阶段，目录自动创建 | P0 |
| 配置继承 | 任务级配置能正确覆盖全局配置 | P0 |
| 暂停恢复 | 能暂停/恢复任务，状态正确保存 | P1 |
| 模板展示 | 右侧面板能展示可用模板 | P1 |

### 7.2 性能指标

- 任务切换时间 < 500ms
- Agent 启动时间 < 3s
- 工作流解析时间 < 100ms
- 界面响应时间 < 16ms (60fps)

---

## 8. 后续规划

### v2.1 - 增强工作流
- 可视化工作流编辑器
- 复杂工具系统（MCP 集成）
- 工作流版本管理

### v2.2 - 协作与同步
- 多人协作
- Git 集成
- 云端备份（可选）

### v3.0 - 平台扩展
- 插件系统
- 社区工作流市场
- 移动端查看

---

## 附录

### A. 配置示例

#### 全局配置

```json
{
  "assets": {
    "templates": "~/notebook/templates",
    "data": "~/notebook/data",
    "references": "~/notebook/references",
    "cases": "~/notebook/cases"
  },
  "workflows": "~/.cospace/workflows",
  "defaultWorkflow": "standard-4stage",
  "agent": {
    "type": "claude",
    "autoStart": true
  }
}
```

#### 工作流模板（standard-4stage.md）

```markdown
# 标准方案工作流（4阶段）

## 元数据
- name: 标准方案
- type: proposal
- version: 1.0

## 阶段定义

### 阶段1：需求确认
- id: demand
- name: 需求确认
- description: 明确客户核心诉求
- outputs:
    - path: 01-需求确认/demand.md
- agent_context: 当前阶段：需求确认...

### 阶段2：框架构思
- id: framework
- name: 框架构思
- description: 确定方案整体结构
- depends: demand
- outputs:
    - path: 02-框架构思/framework.md
- agent_context: 当前阶段：框架构思...

### 阶段3：内容撰写
- id: draft
- name: 内容撰写
- description: 完成方案正文
- depends: framework
- outputs:
    - path: 03-内容撰写/draft.md
- agent_context: 当前阶段：内容撰写...

### 阶段4：审核定稿
- id: review
- name: 审核定稿
- description: 检查并输出最终版本
- depends: draft
- outputs:
    - path: 04-审核定稿/final.docx
- agent_context: 当前阶段：审核定稿...
```

### B. 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+1/2/3 | 切换到第1/2/3个任务 |
| Ctrl+T | 新建任务 |
| Ctrl+S | 保存当前阶段产出 |
| Ctrl+E | 编辑当前阶段文档 |
| Ctrl+W | 关闭当前任务 |
| Ctrl+↑/↓ | 切换阶段 |

---

*文档版本：v2.0*  
*最后更新：2026-05-12*
